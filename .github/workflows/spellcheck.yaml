name: Spellcheck

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  spellcheck:
    name: spellcheck files
    runs-on: ubuntu-slim
    steps:
      - uses: actions/checkout@v7
      - uses: crate-ci/typos@v1.50.0
        with:
          config: ./.typos.toml
          files: "**/*.html"

