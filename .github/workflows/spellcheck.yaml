name: Spellcheck

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  spellcheck:
    name: spellcheck files
    runs-on: ubuntu-slim
    steps:
      - uses: actions/checkout@v4
      - uses: crate-ci/typos@v1.45.1
        with:
          config: ./.typos.toml
          files: "**/*.html"

